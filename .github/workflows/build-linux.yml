name: build-cli-linux

on:
  workflow_dispatch:

jobs:
  build-cli:
    name: Build cli on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [remilia/dev-zack]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: remilia/dev

      - name: Echo test
        run: |
          echo hello
          echo world
