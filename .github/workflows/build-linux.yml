name: build-cli

on:
  workflow_dispatch:

jobs:
  build-cli:
    name: Build cli on ${{ matrix.name }} with ${{ matrix.backend }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: ubuntu_22.04
            runner: sdk-linux-16-core
            backend: llama
          - name: macos_13
            runner: macos-13
            backend: mlx
          - name: macos_14
            runner: macos-14
            backend: mlx
          - name: macos_15
            runner: macos-15
            backend: mlx
          - name: macos_15
            runner: macos-15
            backend: llama

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: remilia/dev

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.24.3
          cache-dependency-path: runner/go.sum

      - name: Build Nexa Cli
        run: make build

      - name: Download runtime
        run: |
          make download BRIDGE_BACKEND=${{ matrix.backend }}
          mv build nexasdk

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: nexasdk_${{ matrix.name }}_${{ matrix.backend }}
          path: ./nexasdk/
